# Deployment status

Deployment account: `062408551112`; region: `us-west-2`; AWS CLI profile: `AdministratorAccess-062408551112`.
Release branch: `main`.
App: https://main.d1t0i4fhkkwocd.amplifyapp.com
API: https://u6suh4aaf5.execute-api.us-west-2.amazonaws.com

AWS infrastructure has been provisioned. Sender, recipient and initial user are `logan.moore@guidedreach.com`. The email identity was already verified and imported. The account is in the SES sandbox, which permits sending to this verified recipient. The Cognito user is confirmed. SNS operations-alert subscription is still pending email confirmation; application SES alerts use the verified recipient independently.

Research now uses **Claude Sonnet 4.6 on Amazon Bedrock**, `us.anthropic.claude-sonnet-4-6`, through the Oregon endpoint and the US inference profile (Oregon, Ohio, N. Virginia). Worker IAM permits exactly this profile and its three regional model resources. AgentCore Web Search uses the existing IAM-authenticated N. Virginia gateway `grs-signal-search-uu51hzcsyq`, connector 1.2.0. No external API key is needed.

The workflow now has two model passes: initial searches and source reads, a Sonnet plan selecting up to two deadline/amendment follow-up searches and document links, then final evidence-based qualification. Discovery searches use the current year without a publication-date gate, so active evergreen procurement pages remain discoverable. Scope relevance ranks ahead of .gov/.edu domains. Unused search allowance covers remaining scope variants, and query strings are archived with evidence. Rechecks preserve access to older notices. Source downloads are limited to 10 attempts, 8 MB each, up to 40 PDF pages/96,000 extracted characters, with up to 6,000 relevant characters supplied per document. Linked procurement/amendment PDFs can be followed even when absent from search results. Scanned, inaccessible or excerpt-only material cannot establish complete verification.

Discovery rejects expired/closed solicitations, out-of-scope buyers, excluded work without a material technology package, and candidates without retained exact evidence. Rechecks can return closed or excluded updates. Existing expired, closed, canceled and awarded records are moved to Filtered out as eligibility is refreshed; user pursuit decisions, notes and restore choices remain intact. Uncertainty is never promoted to verified procurement status.

The US profile rates are **$3.30/M input and $16.50/M output tokens**; search budgeting remains $0.00701/query including Gateway overhead. Planning is capped at 1,500 output tokens and final qualification at 6,500, with combined usage settled once. The existing $1.25/day, $37.50/month and 30-query controls remain. A two-search check reserves $0.27802. These are workload controls and estimates, not invoice caps. [Anthropic Bedrock price list](https://www-cdn.anthropic.com/files/4zrzovbb/website/3684c2faafb97418665782cea0001f439f74b1d2.pdf), [AgentCore pricing](https://aws.amazon.com/bedrock/agentcore/pricing/).

Sonnet access and the two-pass live provider check succeeded. The first full schema exceeded Bedrock's grammar limit; the wire schema now omits format/length/pattern constraints, while strict local validation still enforces them. Planning costs from the rejected request were reconciled. The successful check returned zero suitable current candidates rather than repeating stale findings. Its US-priced estimated cost was $0.1295266 (two searches). Cloud validation exposed AgentCore rejecting fractional seconds in date filters. Date bounds now use whole-second UTC timestamps, with a regression test. Search results without source URLs are safely omitted. Date-only dueAt values are conservatively normalized into dueDate without inventing a time. Discovery no longer receives old listings as candidate suggestions. Rejected date-filter reservations were reconciled to Gateway-only overhead. Final cloud run `run:2026-09-14:sonnet-release-validation` completed at 08:29 UTC with one search, 18,914 combined input tokens, 200 combined output tokens, zero suitable current candidates and estimated cost $0.0727262. It exercised the deployed worker role, date-filtered search, source retrieval, both Sonnet passes, persistence and budget settlement. Daily research is enabled at 06:00 America/Los_Angeles. Normal 30-query and $1.25/day controls are restored. The day ledger, including earlier GPT-OSS tests, is $0.4831208 for 30 searches with zero outstanding reservations. Eight old listings are filtered and one remains needs-verification. Terraform has no drift; the protected state backup is current. Local checks: 76 tests, type checking, formatting and Lambda packaging pass.

Historical blocks: direct OpenAI credentials worked but API credits were exhausted; Terra and GPT-5.4 were denied on Bedrock in both the original and RFP-Tool accounts; GPT-5.5 returned not found; Nova grounding was blocked by SCP. No organization policy was modified.

Terraform state and private inputs remain local, ignored by git, with restricted file permissions. A protected local backup is maintained under `~/.aws/grs-signal-state-backup`. Migrate state to a protected shared backend before a second operator uses Terraform.

The earlier GPT-OSS baseline run completed but produced nine weak or expired records and no verified shortlist. It is retained as history, not evidence of Sonnet research quality. The revised workflow still requires the coverage/quality/cost pilot; empty operational checks do not prove good recall.

Earlier deployed checks passed for API authentication rejection, readiness guard, DynamoDB/GSI/conditional transactions, S3 writes and SES email acceptance. Hosted UI acceptance, SNS confirmation and the seven-day coverage/quality/cost pilot remain separate acceptance steps.

## Coverage correction — September 14, 2026

The earlier zero-result run retrieved unrelated government grants and generic indexes. Fixed current-month/publication gating, domain-first ranking, unused query variants, and an inconsistent mixed-technology exclusion. Busy indexes now prioritize relevant attachments and addenda before link truncation. Explicit internal source URLs support safe direct-source qualification without search charges. Partial or unresolved results cannot retain a verified timestamp.

Live six-search validation `provider:586a91cd-7b54-4a97-b5d2-2b1c2da1a67d` found Southcentral Foundation SCF26-1201 and saved it through normal ingestion; estimate $0.1318035. Buyer eligibility, amendments, and participation requirements remain unresolved. Its uncertain deadline time was removed, retaining only the source date. Direct-source Sonnet validation `provider:c5eefd34-3ff8-404f-bb75-d16cd31e798e` read Clean Energy Alliance's official solicitation page and saved its Data Manager and Customer Contact Center RFP; estimate $0.0484374, no search calls. Both are needs-verification, not a verified shortlist. CEA was independently discovered and supplied as a source URL; it was not found by the six-query AgentCore validation.

One-time local validation allowed six additional searches within the dollar budget; deployed configuration remains 30 queries/day and $1.25/day. Expected day usage is $0.6633617, 36 queries, no outstanding reservations. Normal scheduled research resumes with the next daily budget. Typecheck, 80 regression tests, formatting, and Lambda packaging pass. This improves discovery but does not establish exhaustive recall; the quality pilot remains necessary.

## Deadline read guard — September 14, 2026

Audited the saved records after a report of 2025 bids. University of Arizona L312501 is due April 30, 2025 and is already suppressed/Filtered out. Added a read-time deadline/closed-state guard to view classification: stale saved classifications and manual restores cannot put expired bids in Recommended. Explicit pursuit/submission history remains in Pursuing. Conflicting future dueAt cannot override a past dueDate. Regression checks: 81 tests and typecheck pass. Historical expired records remain available in Filtered out; they are not current leads.
