# Deployment status

Deployment account: `062408551112`; region: `us-west-2`; AWS CLI profile: `AdministratorAccess-062408551112`.
Release branch: `main`.
App: https://main.d1t0i4fhkkwocd.amplifyapp.com
API: https://u6suh4aaf5.execute-api.us-west-2.amazonaws.com

AWS infrastructure has been provisioned. Sender, recipient and initial user are `logan.moore@guidedreach.com`. The email identity was already verified and imported. The account is in the SES sandbox, which permits sending to this verified recipient. The Cognito user is confirmed. SNS operations-alert subscription is still pending email confirmation; application SES alerts use the verified recipient independently.

Research now uses **GPT-OSS 120B on Amazon Bedrock**, `openai.gpt-oss-120b-1:0` in Oregon, plus **AgentCore Web Search** through an IAM-authenticated gateway in N. Virginia. Both services were successfully invoked in account `062408551112`. No external API key or OpenAI credits are required. The earlier OpenAI secret is retained but its worker access permission and environment reference were removed.

The search gateway is `grs-signal-search-uu51hzcsyq`, with Web Search connector version `1.2.0`. Queries retrieve up to five results each. The worker fetches at most eight source documents, reading bounded HTML/text and text-based PDFs (2 MB, first 15 PDF pages, 16,000 characters per document). HTTPS redirects are revalidated, private/metadata IPs are blocked, and the validated DNS address is pinned. Scans, inaccessible sources and failed downloads remain discovery snippets and cannot establish verified procurement state.

The model receives only collected evidence. Bedrock constrained JSON output is enabled and locally schema-validated; a stray opening delimiter observed in runtime output is narrowly repaired. Past-deadline/open-state contradictions are downgraded and cannot be actionable. Conflicting evidence remains conflicting. Returned candidates are schema-validated; evidence excerpts must appear in supplied source text. Snippet-only findings are downgraded and verification timestamps cleared. Empty results are valid when no concrete opportunity is supported. Each inference response and its sources are archived, then the compact result is persisted before the engine receives an ID. Polling reads that result without repeating paid inference. Ambiguous failures retain reservations. One new research request is submitted per scheduled worker invocation.

Rates verified through AWS Pricing API: $0.15/M input tokens and $0.60/M output tokens. AgentCore search is $0.007/query plus Gateway operations; the app conservatively budgets $0.00701/search. Four daily jobs with three to four queries each and the example 40,000-input/8,000-output token workload would be roughly $4–$5/month for model plus search, excluding other AWS infrastructure. The existing $1.25/day, $37.50/month and 30-search-call workload controls remain. These are estimates, not invoice caps.

Sources: [Bedrock pricing](https://aws.amazon.com/bedrock/pricing/), [AgentCore pricing](https://aws.amazon.com/bedrock/agentcore/pricing/), [Web Search connector](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/gateway-target-connector-web-search-tool.html).

Historical blocks: direct OpenAI credentials worked but API credits were exhausted; Terra and GPT-5.4 were denied on Bedrock in both the original and RFP-Tool accounts; GPT-5.5 returned not found; Nova grounding was blocked by SCP. No organization policy was modified.

Terraform state and private inputs remain local, ignored by git, with restricted file permissions. A protected local backup is maintained under `~/.aws/grs-signal-state-backup`. Migrate state to a protected shared backend before a second operator uses Terraform.

Local verification: 71 tests pass, including durable restart behavior, search limits, real-excerpt checks, partial evidence handling, missing usage, private-address blocking and bounded document extraction. Type checking and Lambda packaging pass. Final live provider check completed with two searches, seven fetched documents and one candidate with retained source evidence at estimated cost $0.0180931. It correctly remains unverified because procurement state/deadlines are uncertain. Earlier smoke runs exposed malformed model output and broad-query noise; constrained output, validation and query tuning address these. The operational smoke accepts schema-valid empty/partial findings; it does not certify research coverage or the existence of a currently open opportunity. The first production run, `run:2026-09-14`, completed all four jobs at 07:53 UTC. It processed 10 candidate observations; none met the verified shortlist requirements. One bounded retry occurred before the final parser fix, and archived responses were revalidated without additional inference. The run used 16 searches and approximately $0.1331 of model/search usage. Including deployment checks, the day ledger shows 25 searches, $0.2118896 spent and zero outstanding reservations. Research is enabled for daily 06:00 America/Los_Angeles execution. Terraform reports no drift and the protected state backup is updated.

Earlier deployed checks passed for API authentication rejection, readiness guard, DynamoDB/GSI/conditional transactions, S3 writes and SES email acceptance. Hosted UI acceptance, SNS confirmation and the seven-day coverage/quality/cost pilot remain separate acceptance steps.
